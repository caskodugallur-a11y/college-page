rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Anyone can read any document (public gallery items)
      allow read: if true;
      // Only authenticated users (admins) can write/create/delete
      allow write: if request.auth != null;
    }
  }
}
